#include "main.h"

/**
 * factorial - prints a string, followed by a new line
 * @n: string to print
 *
 * Return: factorial
 */
int factorial(char n)
{
	if(i <= 1)
		return 1;
   	return i * factorial(i - 1);
}
